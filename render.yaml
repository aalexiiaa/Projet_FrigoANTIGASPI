services:
  - type: web
    name: frigo-backend
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn backendFlask:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
